# 轻益点 APP 改造 Harness v1

本文件是本项目后续工作的固定执行协议。之后每次围绕本项目出方案、拆任务、改页面、改接口、检查代码时，都必须优先遵循这里的 harness。

## 1. 固定项目目标

将当前 uni-app/Vue 项目从旧的职业规划/心理健康壳，系统改造为：

**“轻益点体重与代谢健康管理 APP/小程序”。**

用户端产品名优先使用：**轻益点**  
公司/品牌背书使用：**瘦益斐浅**  
核心定位：体重管理、肠道微生态、AKK 菌产品服务、AI 健康顾问、健康档案、打卡随访。

## 2. 固定依据

业务依据优先来自：

`v2-受益斐浅计划书.pdf`

关键业务关键词：

- 全周期健康管理
- 基础服务层、增值服务层、尊享服务层
- 健康自测问卷
- BMI、体重、腰围、体脂、饮食、运动、睡眠、情绪
- AI 智能顾问
- 健康档案
- 趋势报告
- 社区服务点、医院/健康顾问预约
- 用户反馈与数据分析系统
- 产品使用、随访、复购、积分、优惠券

## 3. 每次工作前先判断请求类型

如果用户说“给方案/先别改”：

- 只输出方案，不改文件。

如果用户说“开始改/按方案做/实现/修复”：

- 先读取相关文件，再修改，最后验证。

如果用户说“检查/review”：

- 优先找问题、风险、遗漏和不一致，不急着改。

如果用户只问概念：

- 直接解释，不做文件操作。

## 4. 固定技术约束

项目技术栈：

- Vue 2
- uni-app
- uView / uni-ui
- 页面集中在 `pages/`
- 接口集中在 `common/api/index.js`
- 请求配置在 `common/http/request.js`
- 页面配置在 `pages.json`
- 应用配置在 `manifest.json`

默认不引入新 UI 框架。  
默认复用现有页面结构和样式体系。  
能用现有模块改造，就不重写整套项目。

## 5. 固定改造方向

首页：

- 从“职业测评/职场干货/职业导师/AI职场顾问”
- 改为“健康建档/今日打卡/科普内容/AI健康顾问/顾问随访”。

测评模块：

- 从职业测评改为健康自测、BMI 风险评估、服务层级推荐。

AI 模块：

- 从职业咨询改为轻益点智能健康顾问。

导师模块：

- 从职业导师改为健康顾问、营养师、服务点、合作机构预约。

内容模块：

- 从职场干货改为科学减重、AKK 菌、肠道微生态、饮食运动、睡眠情绪、产品使用指南。

用户中心：

- 增加健康档案、打卡记录、趋势报告、预约记录、积分/优惠券、隐私授权。

## 6. 合规红线

所有健康建议必须使用谨慎表达：

- 可写“辅助”“建议”“参考”“有助于”
- 不写“治愈”“保证减重”“替代药物”“无风险”
- 涉及疾病、用药、严重不适时提示咨询医生
- AI 不能替代医生诊断
- 健康数据需要强调用户授权、隐私保护和数据安全

## 7. 每次输出必须明确

方案类输出包含：

- 改什么
- 为什么改
- 涉及哪些页面/文件
- 需要哪些接口/数据字段
- 分几步做
- 优先级
- 风险点

实现类输出包含：

- 已修改文件
- 核心变化
- 验证方式
- 未完成/需后端配合的部分

## 8. 默认优先级

第一优先级：品牌和业务语义统一  
第二优先级：首页、测评、AI、预约四个核心闭环  
第三优先级：健康档案和打卡趋势  
第四优先级：积分、优惠券、复购、社群运营  
第五优先级：菌群检测、可穿戴设备、B2B 机构后台

## 9. 完成标准

一次改动只有满足以下条件才算完成：

- 页面无明显旧职业规划文案残留
- 业务术语与计划书一致
- 用户路径能闭环
- 接口字段说明清楚
- 健康合规风险已处理
- 能说明如何验证

## 10. 阶段交接规则

整条改造任务的时间线记录在：

`docs/superpowers/plans/2026-07-26-light-benefit-app-roadmap.md`

每个任务阶段结束后，必须明确告诉用户：

- 本阶段完成了什么
- 改了哪些文件
- 如何验证
- 发现的风险或遗留问题
- 下一阶段建议
- 需要用户操作什么

“需要用户操作什么”必须具体到可执行动作，例如：

- 请运行并试走某条路径
- 请确认某组文案
- 请提供 logo/产品图/API 文档
- 请让后端返回某个字段
- 请决定是否进入下一阶段

## 11. 多 Agent 使用与日志规则

当任务可以拆成多个互不冲突的独立模块时，可以主动使用多 agent 并行处理；不需要在执行前单独向用户说明拆分方式。

使用多 agent 时，必须把拆分决策、各 agent 范围、结果、整合情况和验证记录追加到：

`docs/superpowers/logs/agent-delegation-log.md`

当一个较大的任务本可考虑多 agent 但最终没有使用，也应在日志中简要记录原因，例如：

- 写入范围重叠
- 前后依赖明显
- 任务太小
- 需要主线统一产品判断

阶段结束时可以只在最终交接中总结结果；详细多 agent 过程以日志为准，方便用户之后自行查看。
